require("markview").setup({
  markdown = {
    enable = true,
  },
  preview = {
    enable = true,
    modes = { 'n', 'no' ,'c' },
    hybrid_modes = { 'i' },
    linewise_hybrid_mode = true,
  },
});
